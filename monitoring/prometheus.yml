global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "devops-agent"
    static_configs:
      - targets: ["agent:8001"]   # expose /metrics via future prometheus_client if needed
    metrics_path: /metrics

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
